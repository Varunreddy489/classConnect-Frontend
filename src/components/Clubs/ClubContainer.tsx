import MessageInput from "./MessageInput";
import Messages from "./Messages";

const ClubContainer = () => {
  return (
    <div>
      <Messages />
      <MessageInput />
    </div>
  );
};

export default ClubContainer;
